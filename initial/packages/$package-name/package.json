{
  "name": "$package-name",
  "version": "0.0.0-0",
  "description": "",
  "main": "./dist/$package-local-name.js",
  "typings": "./dist/$package-local-name.d.ts",
  "exports": {
    ".": {
      "types": "./dist/$package-local-name.d.ts",
      "default": "./dist/$package-local-name.js"
    }
  },
  "scripts": {
    "init": "npm run init:dev --if-present && npm run init:prod --if-present",
    "init:dev": "npm install --save-dev @compulim/common@0.0.0-0 @happy-dom/global-registrator @testduet/given-when-then @tsconfig/recommended @tsconfig/strictest @types/node esbuild escape-string-regexp expect prettier publint tsup typescript"
  }
}
